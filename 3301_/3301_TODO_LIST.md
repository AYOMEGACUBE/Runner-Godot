# 3301 TODO_LIST

**Актуально:** 2026-04-15. Только актуальные задачи, без выполненных пунктов.

## P0 (release blockers)

- Провести ручной Android RC (`apk/aab`) и device smoke: auth, run-loop, покупки, возвраты сцен.
- Подтвердить production Firebase Rules (`auth.uid` guard) и убрать тестовые открытые правила.

## P1 (стабильность генерации)

- Довести централизованные метрики качества path-генерации (`reject_rate`, `mean_y_trend`, плотность) до стабильных порогов.
- Дотюнить шаги пути / `platform_rules.json` (`height_variation`, gap, bake-лимиты) относительно физики `PhysicsConfig` и камеры.
- Добавить долгий регрессионный прогон генерации на фиксированном seed.
- Подготовить release-режим логирования PathManager (временно включённый `trace_chunk_selection` выключить после локализации проблем прыжка).

## P1 (брендинг)

- Расширить маппинг брендов от color-only к таблице материалов/ресурсов.
- Проверить переключение бренда одной строкой для всех 6 сторон.
- Подтвердить, что брендинг не влияет на физику и collision-конфигурацию.

## P2 (инженерный контур)

- Добавить CI-проверку полноты обязательного комплекта 3301-документации.
- Добавить CI job для headless smoke + regression набора `3301_/3301_test`.
