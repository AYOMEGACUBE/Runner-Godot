# Архитектурные заметки и проблемы производительности

## 📋 Контекст проекта

### Концепция Mega Cube
- **Мегакуб** состоит из **6 сторон** (front, back, left, right, top, bottom)
- Каждая сторона — это **виртуальная стена 720×720 сегментов** (34560×34560 px)
- **Сегмент** = куб **48×48 пикселей**
- У каждого сегмента **6 сторон**, каждая сторона продаётся **отдельно** (концепция MillionDollarHomepage)
- Каждая сторона сегмента может иметь **уникальную картинку** от покупателя

### Текущая архитектура
- `wall.gd` — менеджер стены, создаёт/удаляет видимые сегменты
- `WallSegment.gd` — один сегмент (Node2D + Sprite2D + Area2D)
- `WallData.gd` — хранилище данных (segment_id -> { owner, ... })
- Данные отделены от визуализации

---

## ⚠️ Проблема производительности на мобильных устройствах

### Главная проблема: постоянное создание/удаление сегментов

**Где происходит:**
- `wall.gd::_update_visible_segments()` вызывается при движении камеры
- При каждом обновлении:
  - **Удаляются** сегменты вне видимой области (`segment.queue_free()`)
  - **Создаются** новые сегменты (`segment_scene.instantiate()`) в двойном цикле по X/Y

**Почему это тяжело:**
- Много аллокаций памяти (`instantiate()`)
- Много освобождений памяти (`queue_free()` → сборка мусора)
- Пиковые нагрузки на CPU и GPU → **лаги на слабых телефонах**
- Даже если дыхание выключено, проблема остаётся

**Текущие параметры оптимизации:**
- `UPDATE_INTERVAL = 0.4` сек (до ~2.5 тяжёлых апдейтов в секунду)
- `UPDATE_DISTANCE_THRESHOLD = 256.0` px (обновление при сдвиге камеры)
- `VISIBLE_MARGIN = 2` сегмента (запас за экраном)

---

## ✅ Что уже исправлено

### Удалено централизованное дыхание стены
- **Было:** два уровня движения:
  1. Локальное дыхание каждого сегмента в `WallSegment.gd` (`_update_minimal_animation()`)
  2. Дополнительное покачивание всей сетки в `wall.gd::_process()` (цикл по `active_segments`)
- **Стало:** только локальное дыхание в `WallSegment.gd`
- **Результат:** минус один `sin()` и цикл по всем сегментам каждый кадр
- **Примечание:** это не решает главную проблему лагов (создание/удаление нод)

### Текущее состояние дыхания
- **Локальное дыхание:** `WallSegment.gd::_process()` → `_update_minimal_animation()`
- **Условие:** работает только если `GameState.wall_breathing_enabled == true`
- **Централизованное дыхание:** **УДАЛЕНО** из `wall.gd`

---

## 💡 Обсуждаемые варианты оптимизации

### 1. Реже пересчитывать видимую область ⚠️ РАССМОТРЕНО

**Идея:**
- Увеличить `UPDATE_INTERVAL` с `0.4` до `0.8–1.0` сек
- Увеличить `UPDATE_DISTANCE_THRESHOLD` до `400–500` px (или `viewport_width / 2`)

**Расчёт:**
- Скорость игрока: `350 px/сек` (из `Level.gd` и `Player.gd`)
- За `0.4` сек: `350 * 0.4 ≈ 140 px` (~3 сегмента по 48 px)
- За `1.0` сек: `350 * 1.0 ≈ 350 px` (~7–8 сегментов)
- С учётом `VISIBLE_MARGIN` дыр в стене не будет

**Вердикт:**
- ✅ Поможет **уменьшить частоту** тяжёлых апдейтов
- ❌ **Не решает** проблему массового `instantiate/queue_free` при обновлении
- ⚠️ Лаги останутся, просто будут реже

**Статус:** Требует расчёта и тестирования

---

### 2. Пул сегментов (Object Pooling) ✅ ВОЗМОЖНО

**Идея:**
- Держать фиксированное количество `WallSegment` нод (≈ видимая область + запас)
- При движении камеры **не создавать/удалять**, а:
  - брать сегменты, которые вышли за край
  - переиспользовать их на противоположном краю
  - менять `position`, `segment_id`, вызывать `setup()`
  - подставлять нужную текстуру из `WallData`

**Важно для концепции проекта:**
- ✅ **Совместимо** с идеей "MillionDollarHomepage"
- ✅ Каждый сегмент/сторона по-прежнему может иметь уникальную картинку
- ✅ Данные (`WallData`) остаются отдельно от визуализации
- ✅ При переиспользовании просто меняем `segment_id` и читаем из `WallData` нужный `image_id`/`owner`
- ✅ Визуально игрок видит ту же огромную стену

**Выигрыш:**
- ✅ Нет `instantiate()` и `queue_free()` пачками
- ✅ Нагрузка становится ровной, без пиков
- ✅ Меньше аллокаций и сборки мусора

**Статус:** Рекомендуется как среднесрочное решение

---

### 3. Радикальная архитектура (TileMap / MultiMesh + Shader) 🔄 ДОЛГОСРОЧНО

**Идея:**
- Вместо тысяч нод `WallSegment` использовать:
  - **Вариант A:** Один `TileMap`, где каждая клетка = сегмент 48×48, картинка выбирается по `image_id`
  - **Вариант B:** Один `MeshInstance2D`/`MultiMeshInstance2D` + шейдер, который по координатам берёт нужный фрагмент из атласа

**Данные остаются:**
- `WallData` хранит: `segment_id -> { owner, faces: {front: img_id, back: ..., ...} }`
- Логика продажи/владения (`WallData.buy_side()`) остаётся

**Взаимодействие (клики):**
- Вместо `Area2D` на каждый сегмент:
  - По координате клика `global_pos` вычисляем `(seg_x, seg_y)` и `segment_id`
  - Смотрим в `WallData`, проверяем гейт высоты (CubeView), применяем покупку

**"Дыхание мира":**
- Либо оставить локальную тряску (одна нода на клетку → дороже, но проще)
- Либо перевести в шейдер: смещение UV/вершин по синусу на основе seed от `segment_id`

**Плюсы:**
- ✅ Почти нет `instantiate()` / `queue_free()`
- ✅ Одна (или несколько) батч-отрисовок вместо тысяч отдельных спрайтов
- ✅ Телефон сильно легче дышит

**Что сохраняется:**
- ✅ Мегакуб 720×720 сегментов остаётся
- ✅ Каждая ячейка может иметь уникальную картинку и владельца
- ✅ Концепция "MillionDollarHomepage" не теряется
- ✅ GameState, Level, GameOver, Champions, Profile — почти не трогаем

**Риски:**
- ⚠️ Требуется переписать `wall.gd` и `WallSegment.gd` под новый рендерер
- ⚠️ Нужно реализовать систему кликов по координатам (без `Area2D`)

**Статус:** Долгосрочное решение, требует архитектурного рефакторинга

---

## 🎯 Рекомендуемый план действий

### Краткосрочно (минимум изменений)
1. ✅ Уже сделано: удалено централизованное дыхание стены
2. ⚠️ Подкрутить параметры:
   - `UPDATE_INTERVAL` → `0.8–1.0`
   - `UPDATE_DISTANCE_THRESHOLD` → `400–500` px
3. ⚠️ Протестировать на реальных устройствах

### Среднесрочно (сильный буст, без изменения идеи)
1. ✅ Ввести **пул сегментов** (`WallSegment` нод)
2. ✅ Переиспользовать ноды при движении камеры
3. ✅ Менять `segment_id` и текстуру из `WallData`
4. ✅ Сохранить совместимость с концепцией "MillionDollarHomepage"

### Долгосрочно (радикальная, но правильная архитектура)
1. 🔄 Перейти на **один рендерер стены** (TileMap / MultiMesh + шейдер)
2. 🔄 Оставить всю бизнес-логику в `WallData`/`GameState`
3. 🔄 Клики и покупки считать по координатам, без `Area2D` на каждую клетку

---

## 📝 Важные принципы проекта

### Что НЕЛЬЗЯ терять
- ✅ Концепция мегакуба (6 сторон, 720×720 сегментов)
- ✅ Продажа каждой стороны сегмента отдельно
- ✅ Уникальные картинки на каждой стороне (MillionDollarHomepage)
- ✅ Разделение данных (`WallData`) и визуализации
- ✅ Существующая логика продажи/владения

### Что можно оптимизировать
- ✅ Технику создания/удаления нод (пул вместо instantiate/queue_free)
- ✅ Технику отрисовки (TileMap/MultiMesh вместо тысяч Sprite2D)
- ✅ Частоту обновлений (интервалы и пороги)
- ✅ Локальное дыхание (можно оставить или перевести в шейдер)

---

## 🔍 Текущее состояние кода

### wall.gd
- ✅ Централизованное дыхание **УДАЛЕНО** (нет цикла по `active_segments` в `_process`)
- ⚠️ `_update_visible_segments()` всё ещё создаёт/удаляет сегменты пачками
- ⚠️ Параметры: `UPDATE_INTERVAL = 0.4`, `UPDATE_DISTANCE_THRESHOLD = 256.0`

### WallSegment.gd
- ✅ Локальное дыхание работает (`_update_minimal_animation()`)
- ✅ Условие: `GameState.wall_breathing_enabled`
- ✅ Оптимизация: `VisibleOnScreenNotifier2D` отключает `_process` вне экрана
- ⚠️ `change_side_randomly()` только меняет яркость, не создаёт новые ноды

### WallData.gd
- ✅ Хранит `segments: Dictionary` (segment_id -> { owner })
- ⚠️ Пока только `owner`, но архитектура готова к расширению:
  - `faces: {front: img_id, back: ..., ...}`
  - `image_id` или путь к текстуре

---

## 🚨 КРИТИЧЕСКИ ВАЖНО: Покупки сегментов

### Текущее состояние (ТРЕБУЕТ ИЗМЕНЕНИЯ)
- ❌ **Покупки сейчас возможны в игровом процессе** (`WallSegment.gd::_try_buy()` вызывается при клике на `Area2D`)
- ❌ В `Level.tscn` стена добавлена как дочерний узел, сегменты кликабельны во время игры

### Требование проекта
- ✅ **Покупки сегментов должны быть ТОЛЬКО в CubeView**
- ✅ В игровом процессе (`Level`) стена должна быть **только визуальной**, без возможности покупки
- ✅ Вся логика покупки остаётся в `WallData`, но вызывается только из `CubeView`

### Что нужно изменить
1. **Отключить клики в игровом процессе:**
   - В `wall.gd` при создании сегментов для `Level` отключать `input_pickable` у `Area2D`
   - Или добавить флаг `allow_purchases: bool` в `wall.gd`, который передаётся в сегменты
   - В `CubeView` этот флаг должен быть `true`, в `Level` — `false`

2. **Оставить покупки только в CubeView:**
   - `CubeView` уже имеет логику высотного гейта (`gate_y`)
   - Клики по сегментам в `CubeView` должны работать через `Area2D` или через координаты мыши
   - Проверка высоты (`segment_height <= max_height`) обязательна

---

## 📌 Выводы

1. **Главная проблема:** массовое `instantiate()`/`queue_free()` при движении камеры
2. **Централизованное дыхание:** уже удалено, это хорошо
3. **Пул сегментов:** совместим с концепцией проекта, рекомендуется
4. **Радикальная архитектура:** будет реализована **ВСЁ РАЗОМ** за один проход (не по частям)
5. **Покупки:** должны быть ТОЛЬКО в CubeView, не в игровом процессе
6. **Важно:** сохранить "душу проекта" — мегакуб, продажа сторон, уникальные картинки

---

## 📋 Детали из ТЗ проекта BLACKOUT

### Концепция проекта
- **Название:** BLACKOUT
- **Жанр:** Endless Runner (вертикальный)
- **Платформа:** Android (Godot)
- **Режим:** Offline-first (без серверов на этапе v1)

### Стена (WALL SYSTEM)
- Стена — вертикальное полотно, разбитое на сегменты
- Каждый сегмент содержит: `id`, `высотный диапазон`, `цену (coin)`, `флаг покупки`, `изображение`, `ссылку`, `данные владельца`
- **Ограничение по высоте:** `0 <= segment_height <= max_height игрока` (обязательно)
- **Типы сегментов:**
  - **Free-сегменты:** только в нижней части, покупка за free coin, 1 изображение, 1 ссылка
  - **Paid-сегменты:** любая высота (в пределах max_height), покупка за coin, до 3 смен изображения, смена ссылки разрешена

### Экономика (COIN)
- Одна валюта — `coin`
- Источники: игровой процесс (free), покупка через Billing (paid, позже)
- В коде валюта не разделяется по типу

### Сцена просмотра стены (WALL VIEW / CubeView)
- Отдельная сцена для просмотра всей актуальной стены
- Пользователь может: скроллить стену, видеть купленные сегменты, нажимать на сегменты, переходить по ссылкам
- Источник данных: только `GameState`, без интернета

### UX покупки
- Игрок покупает «сегмент стены», а не валюту
- Отсутствуют слова «донат», «магазин»
- Недостаток coin оформляется как нехватка энергии

### Критерий успеха v1
- Игрок может играть бесконечно
- Игрок может купить сегмент стены
- Стена сохраняется
- Проект масштабируем без переписывания архитектуры

---

## 🔄 План полной переделки архитектуры

### Что будет сделано ВСЁ РАЗОМ:
1. ✅ Переход на радикальную архитектуру (TileMap / MultiMesh + Shader)
2. ✅ Реализация пула сегментов (если потребуется для переходного периода)
3. ✅ Отключение покупок в игровом процессе (`Level`)
4. ✅ Оставление покупок только в `CubeView`
5. ✅ Оптимизация параметров обновления (`UPDATE_INTERVAL`, `UPDATE_DISTANCE_THRESHOLD`)
6. ✅ Сохранение всей бизнес-логики (`WallData`, `GameState`)
7. ✅ Сохранение концепции мегакуба и продажи сторон

### Что НЕ будет затронуто:
- ✅ `GameState` (только расширение, не переписывание)
- ✅ `Level`, `Player`, `Platform`, `Coin` (игровой процесс остаётся)
- ✅ `MainMenu`, `GameOver`, `Champions`, `Profile` (UI остаётся)
- ✅ Логика продажи/владения в `WallData` (остаётся, только расширяется)

---

*Документ обновлён на основе ТЗ проекта BLACKOUT и требований пользователя. Готов к полной переделке архитектуры.*
