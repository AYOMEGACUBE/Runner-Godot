# 📊 Анализ реализуемости P2P-архитектуры для BLACKOUT

**Дата:** 13 февраля 2026  
**Проект:** BLACKOUT (Godot 4.x, Android)  
**Контекст:** Нет бюджета на сервер, нужна децентрализация покупок

---

## ✅ ЧТО РЕАЛЬНО РЕАЛИЗОВАТЬ

### 1. Локальное хранение + синхронизация при подключении
- ✅ **Хранение покупок локально (JSON)** — уже реализовано
- ✅ **Синхронизация при старте/подключении** — реализуемо
- ✅ **Единое игровое время через NTP** — реализуемо (Godot поддерживает HTTP-запросы)

### 2. Решение конфликтов через timestamp + UID
- ✅ **Уникальный ID покупки** — легко добавить (UUID)
- ✅ **Timestamp для порядка** — реализуемо
- ✅ **Проверка дубликатов** — реализуемо

### 3. Минимальная нагрузка на CPU
- ✅ **Хранение только последних N покупок** — реализуемо
- ✅ **Простая репликация без майнинга** — реализуемо

---

## ❌ КРИТИЧЕСКИЕ ПРОБЛЕМЫ

### 1. P2P-сеть в Godot на Android
**Проблема:**
- Godot **не имеет встроенного P2P-стека** (нет Libp2p, WebRTC из коробки)
- Нужны внешние библиотеки (GDExtension) или свой TCP/UDP-слой
- **NAT traversal** сложен без сервера-координатора
- На Android нужны разрешения и фоновые сервисы

**Реальность:**
- Можно использовать **WebSocket** через HTTP (проще, но требует сервер или публичный WebSocket-сервис)
- Прямой P2P (TCP/UDP) между мобильными устройствами **очень сложен** без координатора

### 2. Отсутствие гарантий консистентности
**Проблема:**
- Нет единого источника истины
- Риск рассинхронизации между клиентами
- Нет защиты от подделки локальных данных

**Реальность:**
- Для виртуальных покупок (coin) это приемлемо
- Для реальных денег — **недопустимо**

### 3. Проблема bootstrap (начальная синхронизация)
**Проблема:**
- Новый клиент не знает, с кем соединяться
- Нужен способ найти других пиров (DHT или seed-серверы)
- Без сервера это сложно

**Реальность:**
- Можно использовать публичные DHT (как в BitTorrent), но это сложно для мобильных
- Альтернатива: публичный список пиров (GitHub Gist, IPFS)

### 4. Масштабирование
**Проблема:**
- При росте числа пользователей репликация усложняется
- Хранение только последних N записей может терять старые данные

**Реальность:**
- Для начала проекта это приемлемо
- Позже потребуется миграция на сервер

---

## 💡 РЕКОМЕНДАЦИЯ: ГИБРИДНЫЙ ПОДХОД

### Вариант 1: Локальное + простая синхронизация через публичный API

**Архитектура:**
```
┌─────────────┐
│   Клиент 1  │ ──┐
└─────────────┘   │
                  ├──► [Публичный API] ◄───┐
┌─────────────┐   │   (GitHub Gist /      │
│   Клиент 2  │ ──┘    Firebase Free /    │
└─────────────┘         Supabase Free)    │
                                          │
┌─────────────┐                          │
│   Клиент 3  │ ──────────────────────────┘
└─────────────┘
```

**Преимущества:**
- ✅ Реализуемо в Godot без внешних библиотек
- ✅ Минимальная нагрузка на устройство
- ✅ Единый источник истины (публичный API)
- ✅ Бесплатно (GitHub Gist, Firebase Free Tier, Supabase Free)

**Недостатки:**
- ⚠️ Требует публичный API (но бесплатный)
- ⚠️ Не полностью децентрализовано

**Реализация:**
1. Локальное хранение (уже есть)
2. При покупке → отправка на публичный API
3. При старте → синхронизация с API
4. Разрешение конфликтов через timestamp + UID

**Важно:** Регистрация нужна **ТОЛЬКО разработчику** для создания аккаунта и настройки API. 
Пользователи **НЕ регистрируются** — они просто используют публичный API через HTTP-запросы из приложения.

---

### Вариант 2: Децентрализованное хранилище без P2P

**Архитектура:**
```
┌─────────────┐
│   Клиент 1  │ ──┐
└─────────────┘   │
                  ├──► [IPFS / GunDB] ◄───┐
┌─────────────┐   │   (через HTTP API)    │
│   Клиент 2  │ ──┘                        │
└─────────────┘                            │
                                          │
┌─────────────┐                          │
│   Клиент 3  │ ──────────────────────────┘
└─────────────┘
```

**Преимущества:**
- ✅ Децентрализовано (данные хранятся у всех)
- ✅ Минимальная нагрузка на устройство
- ✅ Нет единой точки отказа

**Недостатки:**
- ⚠️ Требует интеграцию с IPFS/GunDB (GDExtension или HTTP API)
- ⚠️ Сложнее реализация

---

## 🎯 ЧТО МОЖНО СДЕЛАТЬ ПРЯМО СЕЙЧАС

### 1. Улучшить локальное хранение
- ✅ Добавить уникальный ID покупки (UUID)
- ✅ Добавить timestamp синхронизации
- ✅ Подготовить структуру для будущей синхронизации

### 2. Простая синхронизация через публичный API
- ✅ Использовать бесплатные сервисы (GitHub Gist, Firebase Free Tier, Supabase Free)
- ✅ Синхронизация при подключении к интернету
- ✅ Минимальная нагрузка на устройство

### 3. Подготовка к P2P (если нужно)
- ✅ Добавить структуру данных для репликации
- ✅ Реализовать механизм разрешения конфликтов
- ✅ Позже добавить P2P-слой

---

## 📋 ПЛАН РЕАЛИЗАЦИИ (ГИБРИДНЫЙ ПОДХОД)

### Этап 1: Улучшение локального хранения (1-2 дня)
1. Добавить UUID для каждой покупки
2. Добавить timestamp синхронизации
3. Добавить статус покупки (pending/confirmed/rejected)
4. Подготовить структуру для синхронизации

### Этап 2: Простая синхронизация через GitHub Gist (2-3 дня)
1. Создать публичный Gist для хранения покупок
2. Реализовать отправку покупки на Gist (HTTP POST)
3. Реализовать синхронизацию при старте (HTTP GET)
4. Реализовать разрешение конфликтов (timestamp + UID)

### Этап 3: Оптимизация и тестирование (1-2 дня)
1. Оптимизировать синхронизацию (только новые покупки)
2. Добавить кэширование
3. Тестирование на реальных устройствах

**Итого:** 4-7 дней разработки

---

## 🔍 АЛЬТЕРНАТИВЫ

### Альтернатива 1: Firebase Realtime Database (Free Tier)
- ✅ Бесплатно до 1 GB хранения
- ✅ Реальное время синхронизации
- ✅ Простая интеграция через HTTP API
- ⚠️ **Требует регистрацию ТОЛЬКО разработчика** (пользователи не регистрируются)

### Альтернатива 2: Supabase (Free Tier)
- ✅ Бесплатно до 500 MB хранения
- ✅ PostgreSQL база данных
- ✅ REST API из коробки
- ⚠️ **Требует регистрацию ТОЛЬКО разработчика** (пользователи не регистрируются)

### Альтернатива 3: GitHub Gist (бесплатно)
- ✅ Полностью бесплатно
- ✅ Простая интеграция через HTTP API
- ✅ Публичное хранилище
- ⚠️ Ограничение размера файла (1 MB)
- ✅ **Регистрация ТОЛЬКО разработчика** (пользователи используют публичный API без регистрации)

---

## ✅ ИТОГОВЫЙ ВЫВОД

### Полностью децентрализованный P2P без сервера:
- ❌ **Сложно реализовать** в Godot на Android
- ❌ **Рискованно** для продакшена
- ❌ **Требует внешние библиотеки** (GDExtension)

### Гибридный подход (локальное + простая синхронизация):
- ✅ **Реализуемо** прямо сейчас
- ✅ **Безопасно** для продакшена
- ✅ **Минимальная нагрузка** на устройство
- ✅ **Бесплатно** (GitHub Gist, Firebase Free, Supabase Free)
  - ⚠️ **Важно:** Регистрация нужна **ТОЛЬКО разработчику** для создания API/хранилища
  - ✅ **Пользователи НЕ регистрируются** — используют публичный API автоматически через приложение

### Рекомендация:
**Начать с гибридного подхода** (локальное + GitHub Gist/Firebase), а позже, если нужно, добавить P2P-слой.

---

## 📝 СЛЕДУЮЩИЕ ШАГИ

1. ✅ Улучшить локальное хранение (UUID, timestamp, статус)
2. ✅ Реализовать синхронизацию через GitHub Gist
3. ✅ Тестирование на реальных устройствах
4. ⚠️ Позже: добавить P2P-слой (если нужно)

---

*Документ создан на основе анализа переписки с ChatGPT и текущей архитектуры проекта BLACKOUT.*
