# 🎮 BLACKOUT / MEGA CUBE — Текущее состояние проекта

**Дата:** 13 февраля 2026 (обновлено)  
**Версия:** После полной переделки архитектуры + реализация покупок, мини-карты, обваливающихся платформ  
**Платформа:** Godot 4.x, Android

---

## 📋 ОБЩАЯ ИНФОРМАЦИЯ

### Концепция проекта
- **Название:** BLACKOUT
- **Жанр:** Endless Runner (вертикальный)
- **Ключевая особенность:** Мегакуб с системой продажи сегментов (концепция MillionDollarHomepage)
- **Режим:** Offline-first (без серверов на этапе v1)

### Мегакуб
- **Структура:** 6 сторон (front, back, left, right, top, bottom)
- **Размер стороны:** 3,200 × 3,200 сегментов (153,600 × 153,600 px)
- **Размер сегмента:** 48 × 48 пикселей
- **Всего сегментов:** 61,440,000 (6 сторон × 10,240,000 сегментов)
- **Потенциальных продаж:** 368,640,000 (каждая сторона сегмента продаётся отдельно)

---

## ✅ ЧТО СДЕЛАНО

### 1. Оптимизированная архитектура стены (MultiMesh)

#### Проблема (было):
- Создавались тысячи нод `WallSegment` (Node2D + Sprite2D + Area2D)
- При движении камеры массово вызывались `instantiate()` и `queue_free()`
- Пиковые нагрузки на CPU/GPU → лаги на мобильных устройствах

#### Решение (стало):
- **WallRenderer** на основе `MultiMeshInstance2D`
- Один MultiMesh для батч-отрисовки всех видимых сегментов
- Нет создания/удаления нод при движении камеры
- Данные хранятся отдельно в `WallData`

#### Файлы:
- `wall/WallRenderer.gd` — новый оптимизированный рендерер
- `wall/wall.gd` — переписан для использования WallRenderer

#### Параметры оптимизации:
- `UPDATE_INTERVAL`: 0.8 сек (было 0.4)
- `UPDATE_DISTANCE_THRESHOLD`: 400 px (было 256)
- Создаются только видимые сегменты (~200-400 на экране)

---

### 2. Система данных стены (WallData)

#### Структура данных:
```gdscript
segment_id -> {
  "height": float,           # Высота сегмента (Y координата)
  "price": int,              # Цена покупки (10 для free, 50 для paid)
  "faces": {                 # Данные для каждой из 6 сторон
    "front": { "owner": "", "image_id": "", "image_path": "", "link": "" },
    "back": { ... },
    "left": { ... },
    "right": { ... },
    "top": { ... },
    "bottom": { ... }
  },
  "first_owner": "",         # Первый владелец (для истории)
  "purchase_date": int       # Unix timestamp покупки
}
```

#### Методы:
- `get_segment(id)` — получение данных сегмента (создаёт при первом обращении)
- `buy_side(segment_id, side, buyer_uid, coin_cost)` — покупка стороны сегмента
- `get_face_data(segment_id, side)` — данные конкретной стороны
- `set_face_image(segment_id, side, image_path)` — установка картинки
- `set_face_link(segment_id, side, link)` — установка ссылки

#### Цены:
- **Free-сегменты** (y >= 0): 10 coin
- **Paid-сегменты** (y < 0): 50 coin

---

### 3. Покупки сегментов (только в CubeView)

#### Требование:
- Покупки должны быть **ТОЛЬКО в CubeView**, не в игровом процессе

#### Реализация:
- Флаг `allow_purchases: bool` в `wall.gd`
- По умолчанию `false` (Level)
- Включается только в `CubeView` (`allow_purchases = true`)
- Клики обрабатываются по координатам через `handle_click(global_pos)`
- Без `Area2D` на каждый сегмент

#### Ограничение по высоте:
- Игрок может покупать только сегменты в диапазоне: `segment_height <= max_height`
- Проверка в `WallData.buy_side()`
- Высотный гейт в `CubeView` (`gate_y`)

---

### 4. Дыхание мира (независимое движение сегментов)

#### Реализация:
- Централизованное дыхание в `WallRenderer._process()`
- Каждый сегмент имеет **независимые параметры**:
  - `phase` — случайная начальная фаза (0-2π)
  - `speed_factor` — случайная скорость (0.6-1.4)
  - `amplitude_x` — случайная амплитуда по X (0.3-0.8 от базовой)
  - `amplitude_y` — случайная амплитуда по Y (0.5-1.2 от базовой)
  - `offset_x`, `offset_y` — случайные смещения фаз

#### Движение:
- По осям X и Y **независимо** с разными фазами
- Каждый сегмент двигается **асинхронно** со своей скоростью
- Нет синхронного движения — каждый сегмент движется случайно

#### Управление:
- Включается/выключается через `GameState.wall_breathing_enabled`

---

### 5. Смена сторон сегментов

#### Реализация:
- Каждый сегмент имеет свою текущую сторону (`_segment_sides`)
- Независимые таймеры смены сторон (30-90 секунд для каждого сегмента)
- При смене стороны цвет обновляется на соответствующий оттенок бирюзового

#### Цвета сторон (временная визуализация):
- `front` — яркий бирюзовый (0.0, 0.8, 0.7)
- `back` — тёмно-бирюзовый (0.0, 0.5, 0.5)
- `left` — зелёно-бирюзовый (0.2, 0.7, 0.6)
- `right` — голубовато-бирюзовый (0.1, 0.6, 0.8)
- `top` — светло-бирюзовый (0.3, 0.9, 0.8)
- `bottom` — холодный сине-бирюзовый (0.0, 0.4, 0.6)

#### Оптимизация:
- При обновлении видимой области сохраняются текущие стороны сегментов
- Новые сегменты получают случайную начальную сторону

---

### 6. Z-index (стена на заднем плане)

#### Реализация:
- `wall.gd`: `z_index = -10` (стена на заднем плане)
- `WallRenderer` MultiMeshInstance2D: `z_index = -10`
- `Platform.gd`: `z_index = 0` (платформы на переднем плане)
- `Coin.gd`: `z_index = 1` (монеты на переднем плане)

---

### 7. Размер мегакуба увеличен

#### Было:
- `SEGMENTS_PER_SIDE = 720`
- Размер стороны: 720 × 48 = 34,560 px
- Меньше максимального размера мира (147,000 px)

#### Стало:
- `SEGMENTS_PER_SIDE = 3200`
- Размер стороны: 3,200 × 48 = 153,600 px
- Покрывает весь мир с запасом

---

## 🏗️ АРХИТЕКТУРА

### 📁 Дерево проекта (структура файлов и папок):

```
BLACKOUT/
├── 📄 project.godot                    # Конфигурация проекта Godot
├── 📄 icon.svg                        # Иконка проекта
│
├── 📄 GameState.gd                    # ⭐ Autoload Singleton (состояние игры)
├── 📄 MainMenu.gd                     # Скрипт главного меню
├── 📄 MainMenu.tscn                   # Сцена главного меню
├── 📄 CubeView.gd                     # Скрипт просмотра мегакуба (покупки)
├── 📄 CubeView.tscn                   # Сцена просмотра мегакуба
├── 📄 GameOver.gd                     # Скрипт экрана завершения забега
├── 📄 GameOver.tscn                   # Сцена экрана завершения забега
├── 📄 Profile.gd                      # Скрипт профиля игрока
├── 📄 Profile.tscn                    # Сцена профиля игрока
├── 📄 Champions.gd                    # Скрипт таблицы чемпионов
├── 📄 Champions.tscn                  # Сцена таблицы чемпионов
├── 📄 HUD.gd                          # Скрипт игрового интерфейса
├── 📄 LoadingLevel.tscn               # Сцена загрузки уровня
├── 📄 MobileUIScale.gd                # Скрипт масштабирования UI для мобильных
│
├── 📄 Coin.gd                         # Скрипт монеты
├── 📄 Coin.tscn                       # Сцена монеты
├── 📄 Platform.tscn                   # Сцена платформы
│
├── 📁 wall/                           # Система стены (мегакуб)
│   ├── 📄 wall.gd                     # Менеджер стены (использует WallRenderer)
│   ├── 📄 wall.tscn                   # Сцена стены
│   ├── 📄 WallRenderer.gd             # ⭐ Оптимизированный рендерер (MultiMesh)
│   ├── 📄 WallView.gd                 # (не используется)
│   │
│   ├── 📁 data/
│   │   └── 📄 WallData.gd             # ⭐ Хранилище данных сегментов
│   │
│   └── 📁 segment/
│       ├── 📄 WallSegment.gd          # Старый код (не используется в новой архитектуре)
│       └── 📄 WallSegment.tscn        # Старая сцена (не используется)
│
├── 📁 scripts/                        # Игровые скрипты
│   ├── 📄 Level.gd                    # Скрипт игрового уровня
│   ├── 📄 Player.gd                    # Скрипт игрока
│   ├── 📄 Platform.gd                 # Скрипт платформы (включая обваливающиеся)
│   └── 📄 MinimapDrawer.gd            # Скрипт отрисовки мини-карты
│
├── 📁 heroes/                         # Ресурсы героев
│   ├── 📄 hero_default.png
│   ├── 📄 hero_default_jump_frames.tres
│   ├── 📄 hero_monster.png
│   ├── 📄 hero_monster_jump_frames.tres
│   ├── 📄 hero_red.png
│   ├── 📄 hero_red_jump_frames.tres
│   ├── 📄 hero_blue.png
│   ├── 📄 hero_blue_jump_frames.tres
│   ├── 📄 hero_orange.png
│   └── 📄 hero_orange_jump_frames.tres
│
├── 📁 .godot/                         # Служебные файлы Godot (кэш, импорты)
│
├── 📄 level.tscn                      # ⭐ Главная игровая сцена (Endless Runner)
│
└── 📄 Документация/
    ├── 📄 ARCHITECTURE_NOTES.md       # Архитектурные заметки
    ├── 📄 REVENUE_CALCULATION.md      # Расчёт потенциального дохода
    ├── 📄 PROJECT_STATE.md            # Текущее состояние проекта (этот файл)
    └── 📄 концептуально-архитектурное ТЗ.txt  # Полное техническое задание
```

**Легенда:**
- ⭐ — ключевые файлы архитектуры
- 📄 — файл
- 📁 — папка

---

### 🎬 Структура сцен и их связи:

#### Иерархия сцен (граф переходов):

```
                    ┌─────────────────┐
                    │   MainMenu.tscn │  ← Стартовая сцена (project.godot)
                    │   (MainMenu.gd)  │
                    └────────┬────────┘
                             │
        ┌────────────────────┼────────────────────┬──────────────────┐
        │                    │                    │                  │
        ▼                    ▼                    ▼                  ▼
┌──────────────┐   ┌──────────────┐   ┌──────────────┐   ┌──────────────┐
│  level.tscn  │   │ Profile.tscn │   │Champions.tscn│   │ CubeView.tscn│
│ (Level.gd)   │   │ (Profile.gd) │   │(Champions.gd)│   │(CubeView.gd) │
└──────┬───────┘   └──────────────┘   └──────────────┘   └──────┬───────┘
       │                                                          │
       │ (Player._die())                                          │
       ▼                                                          │
┌──────────────┐                                                  │
│GameOver.tscn │ ────────────────────────────────────────────────┘
│(GameOver.gd) │
└──────┬───────┘
       │
       ├──→ MainMenu.tscn (кнопка "Main Menu")
       ├──→ level.tscn (кнопка "Restart Run")
       └──→ CubeView.tscn (кнопка "View Cube")
```

#### Детальное описание сцен:

##### 1. **MainMenu.tscn** (Главное меню)
- **Скрипт:** `MainMenu.gd`
- **Тип:** `Control`
- **Назначение:** Точка входа в игру, навигация по основным разделам
- **Кнопки:**
  - **Play** → `level.tscn` (требует nickname)
  - **Profile** → `Profile.tscn`
  - **Champions** → `Champions.tscn`
  - **Cube View** → `CubeView.tscn`
- **Особенности:**
  - Проверяет наличие nickname перед запуском игры
  - Отображает текущий аватар игрока
  - Показывает текущий nickname

##### 2. **level.tscn** (Игровой уровень)
- **Скрипт:** `scripts/Level.gd`
- **Тип:** `Node2D`
- **Назначение:** Основной игровой процесс (Endless Runner)
- **Дочерние ноды:**
  ```
  Level (Node2D)
  ├── Platforms (Node2D)          # Контейнер для платформ
  ├── Wall (Node2D)                # ⭐ Инстанс wall.tscn (мегакуб)
  │   ├── WallData (WallData)     # Данные сегментов
  │   └── WallRenderer (Node2D)   # MultiMesh рендерер
  │       └── MultiMeshInstance2D # Визуализация сегментов
  ├── Player (CharacterBody2D)     # Игрок
  │   ├── Camera2D                # Камера следует за игроком
  │   ├── CollisionShape2D         # Коллизия игрока
  │   └── AnimatedSprite2D        # Анимация героя
  └── HUD (CanvasLayer)            # Игровой интерфейс
      └── ... (UI элементы)
  ```
- **Особенности:**
  - Платформы генерируются процедурно
  - Стена отображается на заднем плане (`z_index = -10`)
  - При смерти игрока → переход в `GameOver.tscn`
  - Покупки сегментов **отключены** (`allow_purchases = false`)

##### 3. **CubeView.tscn** (Просмотр мегакуба)
- **Скрипт:** `CubeView.gd`
- **Тип:** `Node2D`
- **Назначение:** Просмотр и покупка сегментов мегакуба
- **Дочерние ноды:**
  ```
  CubeView (Node2D)
  ├── Camera2D                     # Камера для навигации по стене
  ├── GateLine (Line2D)            # Визуальная линия высотного гейта
  ├── UILayer (CanvasLayer)        # UI слой
  │   └── Panel
  │       └── VBoxContainer
  │           ├── TitleLabel
  │           └── BackButton       # Возврат в MainMenu
  └── Wall (Node2D)                # ⭐ Инстанс wall.tscn (тот же, что в Level)
      ├── WallData (WallData)      # Данные сегментов
      └── WallRenderer (Node2D)    # MultiMesh рендерер
          └── MultiMeshInstance2D  # Визуализация сегментов
  ```
- **Особенности:**
  - Покупки сегментов **включены** (`allow_purchases = true`)
  - Высотный гейт: можно покупать только сегменты ниже достигнутой высоты
  - Клики обрабатываются по координатам (без Area2D)
  - Возврат в главное меню через кнопку "Back to Menu"

##### 4. **GameOver.tscn** (Экран завершения забега)
- **Скрипт:** `GameOver.gd`
- **Тип:** `CanvasLayer`
- **Назначение:** Показ результатов забега и выбор дальнейших действий
- **Дочерние ноды:**
  ```
  GameOver (CanvasLayer)
  ├── GameOver#MobileScale (Node)  # Масштабирование для мобильных
  └── Panel (Panel)
      └── VBox (VBoxContainer)
          ├── HeightLabel          # Максимальная достигнутая высота
          ├── ScoreLabel           # Набранные очки
          └── Buttons (HBoxContainer)
              ├── ViewCubeButton   # → CubeView.tscn
              ├── RestartButton    # → level.tscn
              └── MainMenuButton   # → MainMenu.tscn
  ```
- **Особенности:**
  - Показывает результаты из `GameState.last_run_score` и `GameState.last_run_max_height`
  - Три варианта действий: просмотр куба, рестарт забега, главное меню

##### 5. **Profile.tscn** (Профиль игрока)
- **Скрипт:** `Profile.gd`
- **Тип:** `Control`
- **Назначение:** Настройка nickname и аватара игрока
- **Особенности:**
  - Установка nickname (обязательно для игры)
  - Выбор героя (default, monster, red, blue, orange)
  - Настройка кастомного аватара (jump0/jump1)

##### 6. **Champions.tscn** (Таблица чемпионов)
- **Скрипт:** `Champions.gd`
- **Тип:** `Control`
- **Назначение:** Отображение таблицы лучших игроков
- **Особенности:**
  - Показывает топ игроков из `GameState.champions`
  - Данные сохраняются в `GameState`

##### 7. **wall.tscn** (Сцена стены)
- **Скрипт:** `wall/wall.gd`
- **Тип:** `Node2D`
- **Назначение:** Универсальная сцена стены (используется в Level и CubeView)
- **Дочерние ноды:**
  ```
  Wall (Node2D)
  ├── WallData (WallData)          # Хранилище данных сегментов
  └── WallRenderer (Node2D)        # Рендерер на MultiMesh
      └── MultiMeshInstance2D      # Визуализация сегментов
  ```
- **Особенности:**
  - Используется как в `level.tscn`, так и в `CubeView.tscn`
  - Поведение зависит от флага `allow_purchases`
  - В Level: только визуализация (покупки отключены)
  - В CubeView: визуализация + покупки (покупки включены)

#### Автозагрузка (Autoload):

##### **GameState** (Singleton)
- **Файл:** `GameState.gd`
- **Тип:** `Node`
- **Назначение:** Глобальное состояние игры
- **Хранит:**
  - Профиль игрока (nickname, player_uid, selected_hero_id)
  - Игровые данные (score, max_height_reached, best_score)
  - Таблицу чемпионов (champions)
  - Настройки (wall_breathing_enabled, disable_wall)
- **Доступ:** `GameState.property_name` из любой сцены

#### Связи между сценами:

1. **MainMenu → Level:**
   - Проверка nickname → запуск игры
   - `GameState.start_new_run()` → сброс состояния забега

2. **Level → GameOver:**
   - При смерти игрока (`Player._die()`)
   - Сохранение результатов в `GameState.last_run_*`

3. **GameOver → Level:**
   - Кнопка "Restart Run"
   - `GameState.start_new_run()` → новый забег

4. **GameOver → CubeView:**
   - Кнопка "View Cube"
   - Просмотр мегакуба после забега

5. **GameOver → MainMenu:**
   - Кнопка "Main Menu"
   - Возврат в главное меню

6. **MainMenu ↔ Profile:**
   - Взаимные переходы
   - Настройка профиля перед игрой

7. **MainMenu ↔ Champions:**
   - Взаимные переходы
   - Просмотр таблицы лидеров

8. **MainMenu ↔ CubeView:**
   - Взаимные переходы
   - Просмотр мегакуба в любое время

#### Общие компоненты:

- **wall.tscn** — используется в `level.tscn` и `CubeView.tscn`
- **GameState** — доступен из всех сцен через Autoload
- **Platform.tscn** и **Coin.tscn** — инстансы создаются процедурно в `Level.gd`

---

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ

### WallRenderer (MultiMesh)

#### Создание mesh:
- Программно создаётся `ArrayMesh` с квадратом 48×48
- Используется для всех сегментов (батч-отрисовка)

#### Управление видимостью:
- `update_visible_area(min_x, max_x, min_y, max_y)` — обновляет видимую область
- Сохраняет состояние сторон сегментов при обновлении
- Обновляет только цвета и трансформы, не создаёт новые ноды

#### Дыхание:
- В `_process()` применяется независимое движение к каждому сегменту
- Параметры генерируются на основе `segment_id` (детерминированно)

#### Смена сторон:
- В `_process_side_changes()` обрабатываются таймеры для каждого сегмента
- При смене стороны обновляется цвет через `set_instance_color()`

---

### wall.gd (Менеджер стены)

#### Обновление видимой области:
- Вычисляет границы видимой области по позиции камеры
- Вызывает `wall_renderer.update_visible_area()`
- Нет создания/удаления нод

#### Обработка кликов:
- `handle_click(global_pos)` — делегирует в `WallRenderer`
- Используется только в `CubeView`

---

### CubeView (Покупки сегментов)

#### Обработка кликов:
- `_input()` обрабатывает клики/тачи
- Преобразует координаты в `segment_id` через `handle_click()`
- Проверяет высотный гейт (`gate_y`)
- Вызывает `_try_purchase_segment()`

#### Покупка:
- Получает `wall_data` из `wall_instance`
- Вызывает `wall_data.buy_side()`
- Обновляет визуал через `wall_instance.update_segment_visual()`

---

### WallData (Данные стены)

#### Структура:
- `segments: Dictionary` — хранит данные всех сегментов
- Lazy initialization — сегмент создаётся при первом обращении
- Вычисляет высоту и цену из `segment_id`

#### Покупка:
- Проверяет высотный гейт (`segment_height <= max_height`)
- Проверяет достаточность монет
- Списывает монеты из `GameState.score`
- Сохраняет владельца и дату покупки

---

## 📊 ПРОИЗВОДИТЕЛЬНОСТЬ

### Оптимизации:

1. **MultiMesh вместо тысяч нод:**
   - Было: тысячи `Node2D` с `Sprite2D` и `Area2D`
   - Стало: один `MultiMeshInstance2D` для всех видимых сегментов

2. **Нет создания/удаления нод:**
   - Было: `instantiate()` и `queue_free()` при движении камеры
   - Стало: обновление трансформов и цветов в MultiMesh

3. **Lazy loading:**
   - Создаются только видимые сегменты (~200-400 на экране)
   - Не зависит от общего количества сегментов (61,440,000)

4. **Оптимизированные интервалы:**
   - `UPDATE_INTERVAL`: 0.8 сек (было 0.4)
   - `UPDATE_DISTANCE_THRESHOLD`: 400 px (было 256)

5. **Централизованное дыхание:**
   - Один `_process()` в `WallRenderer`
   - Независимые параметры для каждого сегмента

---

## 🎯 КЛЮЧЕВЫЕ ОСОБЕННОСТИ

### Что работает:

1. ✅ **Визуализация стены:**
   - Стена отображается через MultiMesh
   - Дыхание мира (независимое движение сегментов)
   - Смена сторон сегментов (разные оттенки бирюзового)

2. ✅ **Покупки сегментов:**
   - Только в CubeView (не в игровом процессе)
   - Ограничение по высоте (`segment_height <= max_height`)
   - Проверка достаточности монет
   - Обновление визуала после покупки

3. ✅ **Данные:**
   - Хранение данных сегментов в `WallData`
   - Поддержка всех 6 сторон каждого сегмента
   - Готовность к загрузке картинок и ссылок

4. ✅ **Производительность:**
   - Оптимизированная архитектура для мобильных устройств
   - Нет массовых аллокаций при движении камеры
   - Ровная нагрузка без пиков

---

## 📝 ЧТО СДЕЛАНО ДОПОЛНИТЕЛЬНО И ЧТО ЕЩЁ НЕ ЗАВЕРШЕНО

### Уже реализовано (поверх базовой архитектуры)

1. ✅ **Сохранение данных стены (`WallData`)**  
   - Добавлен JSON‑сейв `user://wall_segments.json`.  
   - Методы `save_to_file()` / `load_from_file()` в `WallData.gd`.  
   - Автосохранение при покупке, установке картинки и ссылок.  
   - Загрузка данных при старте стены (`wall.gd` создаёт `WallData` и вызывает `load_from_file()`).

2. ✅ **UI покупки сегмента в `CubeView`**  
   - При клике по сегменту открывается диалог `PurchaseDialog`:  
     - показывает цену и проверяет, хватает ли монет,  
     - позволяет выбрать сторону (front/back/left/right/top/bottom),  
     - даёт ввести ссылку,  
     - даёт выбрать изображение с диска.  
   - После подтверждения:
     - вызывается `buy_side()` в `WallData`,  
     - картинка копируется в `user://wall_images/` и путь сохраняется в `image_path`,  
     - ссылка сохраняется через `set_face_link()`,  
     - визуал сегмента обновляется через `wall_instance.update_segment_visual()`.

3. ✅ **Базовая валидация ссылок**  
   - В `WallData.gd` реализован `_validate_link_format(url)` (формат `http://` / `https://`, отсутствие пробелов).  
   - `set_face_link()` отклоняет пустые и некорректные URL, автосохраняет при успехе.

4. ✅ **Инфраструктура для изображений**  
   - `WallData` хранит `image_path` для каждой стороны.  
   - `CubeView.gd` копирует выбранные пользователем файлы в `user://wall_images/…` и регистрирует путь в `WallData`.  
   - Добавлен метод `get_face_image_path()` для удобного доступа к пути изображения.

5. ✅ **Гибридный рендер изображений**  
   - В `WallRenderer.gd` реализован пул `Sprite2D` для отображения изображений поверх MultiMesh.  
   - Создан отдельный слой `_images_layer` с `z_index = -9` для спрайтов.  
   - Методы `_get_or_create_sprite()`, `_release_sprite()`, `_update_image_sprites()` для управления пулом.  
   - Спрайты позиционируются синхронно с движением MultiMesh (включая дыхание).

6. ✅ **Открытие новых сторон мегакуба**  
   - В `GameState.gd` добавлены `unlocked_sides` и `active_wall_side`.  
   - Методы `unlock_next_wall_side()`, `get_active_wall_side()`, `get_unlocked_wall_sides()`.  
   - Базовая логика открытия следующей стороны при достижении порога покупок (50 сегментов) в `CubeView.gd`.  
   - Сохранение прогресса в `GameState.save_scores()`.

7. ✅ **Расширенный UX покупки и навигации**  
   - **Подсветка выбранного сегмента:** В `WallRenderer.gd` добавлена визуальная подсветка (белый оттенок) при клике на сегмент.  
   - **Статус в диалоге:** В `PurchaseDialog` добавлен `StatusLabel` с информацией о владельце и дате покупки.  
   - **Зум камеры:** В `CubeView.gd` реализован зум колесом мыши (от 0.1x до 5.0x).  
   - **Перетаскивание камеры:** Реализовано перетаскивание правой кнопкой мыши или тач-жестами.  
   - **Мини-карта:** Добавлена мини-карта в правом верхнем углу `CubeView` с визуализацией стены, купленных сегментов и позиции камеры (`scripts/MinimapDrawer.gd`).

8. ✅ **Улучшение геймплея раннера**  
   - **Обваливающиеся платформы:** Добавлен тип платформ, которые обваливаются через 0.75 сек после приземления игрока.  
     - Генерируются с частотой 30% при старте, до 60% на высоте.  
     - Отключают коллизию сразу после первого касания, чтобы игрок не мог опереться второй раз.  
   - **Угол подъёма платформ:** Уменьшен с ~30% (`DY_STEP = 64px`) до ~5% (`DY_STEP = 10px`) для лучшей видимости сегментов стены.  
   - **Формула роста цены по высоте:** Цена сегмента увеличивается на 10% каждые 1000 пикселей высоты (максимум 3x на высоте 20000+ px).

9. ✅ **Исправление критических ошибок**  
   - Исправлен краш после исчезновения платформ (безопасное удаление из массива `platforms`).  
   - Исправлена ошибка парсера в `CubeView.gd` (проблема с типом переменной `side`).  
   - Улучшена диагностика создания диалога покупки (детальные сообщения об ошибках).

### Что всё ещё НЕ завершено

1. ⚠️ **Фактическое отображение картинок на стене**  
   - Пути к изображениям уже сохраняются, но полноценный рендер всех купленных картинок поверх MultiMesh ещё не доведён до продакшн‑состояния.  
   - Нужен аккуратный гибридный рендер (`MultiMesh` + `Sprite2D`/шейдер) с учётом производительности.

2. ⚠️ **Открытие новых сторон куба**  
   - Структура данных для 6 сторон есть, но:
     - нет подсчёта прогресса по стороне,  
     - нет условий открытия новой стороны (процент выкупа / достигнутая высота),  
     - нет сохранения списка открытых сторон в `GameState`,  
     - нет UI‑индикатора прогресса открытия сторон.

3. ⚠️ **Расширенный UX покупки**  
   - Диалог есть, но:
     - не показываются текущий владелец и статус ячейки,  
     - нет подсветки выбранного сегмента на сцене,  
     - нет анимаций/звуков успешной/неуспешной покупки,  
     - нет “энергетического” UX для нехватки монет (по ТЗ).

4. ⚠️ **Переход по ссылкам сегментов**  
   - Ссылки можно задать и сохранить, но при клике по уже купленному сегменту ссылка пока не открывается через `OS.shell_open()`.  
   - Нет предупреждения пользователю перед открытием внешнего URL.

5. ⚠️ **Глубина геймплея и связка со стеной**  
   - Endless‑runner реализован на базовом уровне (прыжки, платформы, монеты, смерть, рекорд), но:  
     - нет сложных/движущихся/исчезающих платформ,  
     - нет множителей/комбо/усилителей,  
     - нет прямой связи высоты и экономики стены (цены/скидки/бонусы от прогресса).

6. ⚠️ **Удобная навигация по стене**  
   - Камера в `CubeView` базовая:  
     - нет зума,  
     - нет плавного перетаскивания/скролла,  
     - нет быстрых переходов (к началу, к `max_height`, к своим сегментам),  
     - нет мини‑карты стены.

Дальнейшие задачи по игре и серверной части сведены в отдельный план ниже.

---

## 🔄 ИЗМЕНЕНИЯ ПО СРАВНЕНИЮ С ИСХОДНОЙ АРХИТЕКТУРОЙ

### Удалено:
- ❌ Создание тысяч нод `WallSegment` при движении камеры
- ❌ Централизованное дыхание стены в `wall.gd` (было два уровня движения)
- ❌ `Area2D` на каждый сегмент для кликов (теперь координаты)

### Добавлено:
- ✅ `WallRenderer` на основе MultiMesh
- ✅ Система кликов по координатам
- ✅ Независимое дыхание каждого сегмента
- ✅ Смена сторон сегментов с визуализацией
- ✅ Расширенная структура данных в `WallData`

### Изменено:
- 🔄 `wall.gd` переписан для использования `WallRenderer`
- 🔄 `CubeView` использует координаты вместо `Area2D`
- 🔄 Размер мегакуба увеличен с 720×720 до 3200×3200
- 🔄 Параметры обновления оптимизированы

---

## 📈 МЕТРИКИ ПРОЕКТА

### Размеры:
- **Сторона мегакуба:** 3,200 × 3,200 сегментов = 153,600 × 153,600 px
- **Всего сегментов:** 61,440,000
- **Потенциальных продаж:** 368,640,000 (каждая сторона сегмента)
- **Потенциальный доход:** ~11 миллиардов coin

### Производительность:
- **Видимых сегментов на экране:** ~200-400 (зависит от размера экрана)
- **Обновление:** каждые 0.8 сек или при сдвиге камеры на 400 px
- **Дыхание:** один `_process()` для всех видимых сегментов
- **Смена сторон:** независимо для каждого сегмента (30-90 сек)

---

## 🎮 ИГРОВОЙ ПРОЦЕСС

### Что работает:
1. ✅ Игрок прыгает по платформам
2. ✅ Собирает монеты
3. ✅ Стена отображается на заднем плане
4. ✅ Дыхание мира работает (независимое движение каждого сегмента)
5. ✅ Смена сторон сегментов видна (разные оттенки бирюзового)
6. ✅ Покупки сегментов в CubeView (с диалогом, выбором стороны, изображения и ссылки)
7. ✅ Обваливающиеся платформы (30-60% в зависимости от высоты)
8. ✅ Формула роста цены сегментов по высоте
9. ✅ Визуальная подсветка выбранного сегмента
10. ✅ Зум и перетаскивание камеры в CubeView
11. ✅ Мини-карта стены с визуализацией

### Что не работает (не реализовано):
1. ⚠️ Покупка сегментов в игровом процессе (отключено намеренно по ТЗ)
2. ⚠️ Открытие ссылок при клике на купленные сегменты
3. ⚠️ Анимации и звуки покупки
4. ⚠️ Быстрые переходы в CubeView (к началу, к max_height, к своим сегментам)

---

## 🔐 СОХРАНЕНИЕ ДАННЫХ

### GameState (Autoload):
- Сохраняет: nickname, best_score, champions, selected_hero_id, custom_avatar, wall_breathing_enabled
- Путь: `user://blackout_run_scores.save`
- Формат: Dictionary через `store_var()`
 
### WallData:
- Хранит данные сегментов в `segments: Dictionary` (lazy‑инициализация только затронутых сегментов)
- Сохраняется в JSON‑файл `user://wall_segments.json`
- Автосохранение при:
  - покупке стороны (`buy_side()`),
  - установке картинки (`set_face_image()`),
  - сохранении ссылки (`set_face_link()`).
- Загружается при старте стены (`wall.gd` создаёт `WallData` и вызывает `load_from_file()`)
- Готово к последующему переносу на SQLite/сервер (формат данных уже структурирован)

---

## 🚀 ГОТОВНОСТЬ К РАСШИРЕНИЮ

### Готово:
- ✅ Архитектура для загрузки картинок (`set_face_image()`)
- ✅ Архитектура для ссылок (`set_face_link()`)
- ✅ Заглушки для мультиплеера (`player_uid`, `auth_provider`, `auth_token`)
- ✅ Система открытия новых сторон (структура данных готова)

### Нужно добавить:
- ⚠️ UI для загрузки картинок
- ⚠️ UI для ввода ссылок
- ⚠️ Проверка доступности ссылок (HTTP request)
- ⚠️ Сохранение `WallData` в файл
- ⚠️ Логика открытия новых сторон куба

---

## 📋 ВАЖНЫЕ ПРИНЦИПЫ

### Что сохранено:
- ✅ Концепция мегакуба (6 сторон, продажа каждой стороны сегмента отдельно)
- ✅ Уникальные картинки на каждой стороне (MillionDollarHomepage)
- ✅ Разделение данных (`WallData`) и визуализации
- ✅ Вся бизнес-логика в `WallData`/`GameState`
- ✅ Игровой процесс (`Level`, `Player`, `Platform`, `Coin`)

### Что оптимизировано:
- ✅ Техника отрисовки (MultiMesh вместо тысяч Sprite2D)
- ✅ Техника создания/удаления (обновление трансформов вместо instantiate/queue_free)
- ✅ Частота обновлений (интервалы и пороги)
- ✅ Дыхание мира (независимое движение каждого сегмента)

---

## 🐛 ИЗВЕСТНЫЕ ОГРАНИЧЕНИЯ

1. **Отображение картинок на стене ограничено**  
   - Пути к изображениям есть и сохраняются, но полноценный рендер всех купленных картинок поверх MultiMesh ещё не доведён до продакшн‑состояния.  
   - Нужна аккуратная реализация (пул спрайтов / атлас / шейдер) с учётом производительности.
   
2. **Переход по ссылкам не реализован**  
   - Ссылки можно сохранить и валидировать по формату, но при клике по уже купленному сегменту ссылка пока не открывается через `OS.shell_open()`.  
   - Нет защиты/предупреждения при открытии внешних URL.
   
3. **Открытие новых сторон куба отсутствует**  
   - Только одна активная сторона, нет прогресс‑системы, нет условий открытия следующих сторон.
   
4. **Глубина геймплея и экономических связок**  
   - Игровой цикл минимален, нет продвинутых механик риска/прогресса/связки высоты и цен сегментов.  
   - Экономика (динамическая цена, бонусы за ранние покупки и т.п.) пока в зачатке.

---

## 📚 ДОКУМЕНТАЦИЯ

### Созданные документы:
- `ARCHITECTURE_NOTES.md` — архитектурные заметки и проблемы производительности
- `REVENUE_CALCULATION.md` — расчёт потенциального дохода
- `PROJECT_STATE.md` — текущее состояние проекта (этот документ)

### ТЗ:
- `концептуально-архитектурное ТЗ.txt` — полное техническое задание проекта

---

## ✅ ИТОГОВЫЙ СТАТУС

### Готово к использованию:
- ✅ Оптимизированная архитектура стены
- ✅ Визуализация мегакуба
- ✅ Дыхание мира
- ✅ Смена сторон сегментов
- ✅ Покупки сегментов в CubeView
- ✅ Ограничение по высоте
- ✅ Проверка достаточности монет
 - ✅ Локальное (offline) сохранение данных стены и покупок

### Требует доработки:
- ⚠️ Финальное отображение картинок на стене (Sprite2D/шейдер поверх MultiMesh)
- ⚠️ Логика открытия новых сторон куба
- ⚠️ Расширенный UX покупки и навигации в CubeView
- ⚠️ Интеграция ссылок (открытие в браузере, защита)

### Производительность:
- ✅ Оптимизировано для мобильных устройств
- ✅ Нет массовых аллокаций
- ✅ Ровная нагрузка без пиков
- ✅ Готово к масштабированию

---

## 🗺️ ПЛАН ДАЛЬНЕЙШЕЙ РЕАЛИЗАЦИИ (ИГРА VS СЕРВЕР)

### 1. Всё, что касается ИГРЫ (offline)

- **Стена и визуал:**
  - Завершить рендер картинок поверх MultiMesh (гибрид MultiMesh + Sprite2D/шейдер).  
  - Оптимизировать хранение и загрузку текстур (кэш, ленивые загрузки).  
  - Реализовать открытие новых сторон мегакуба (прогресс по выкупу и/или высоте, сохранение в `GameState`, UI‑индикатор).

- **Покупки и UX:**
  - Расширить диалог покупки: показать статус ячейки (свободна/куплена, кем и когда), высоту, прогресс стороны.  
  - Добавить подсветку выбранного сегмента на стене, анимации и звуки покупки/ошибки.  
  - Ввести “энергетический” UX для нехватки монет (вместо жёстких ошибок).

- **Геймплей раннера:**
  - Добавить более сложные типы платформ (движущиеся, исчезающие, с риском).  
  - Ввести прогресс‑механики: множители очков, комбо, временные бустеры.  
  - Связать высоту/результат забега с экономикой стены (доступ, цены, скидки, бонусы).

- **Экономика и ограничения:**
  - Чётко зафиксировать и визуализировать границу free/paid зоны.  
  - Ограничить обновления: free‑сегменты — 1 картинка + 1 ссылка, paid‑сегменты — ограниченное число замен.  
  - Добавить пакетную покупку сегментов (выбор нескольких ячеек и одна транзакция).

- **UX и мобильная оптимизация:**
  - Улучшить навигацию в `CubeView` (зум, скролл, быстрые переходы, мини‑карта).  
  - Добавить настройки качества графики, опции отключения дыхания/эффектов на слабых устройствах.  
  - Протестировать игру на реальных Android‑устройствах и дотюнить производительность/интерфейс.

- **Качество кода и тесты:**
  - Написать unit‑тесты для ключевых участков логики (`WallData.buy_side`, сохранение/загрузка, высотный гейт).  
  - Очистить проект от устаревших скриптов (`WallSegment.gd`, `WallView.gd` и т.п., либо вынести в `archive/`).

### 2. Всё, что касается СЕРВЕРА / ОНЛАЙН‑ЧАСТИ

- **Хранилище и верификация:**
  - Ввести серверное хранилище стены (БД/Backend), синхронизированное с локальным `WallData`.  
  - Реализовать онлайн‑верификацию покупок (подтверждение владения сегментами на сервере).  
  - Обеспечить уникальность владения, защиту от подмены локальных сейвов (анти‑фрод).

- **Монетизация:**
  - Интегрировать Google Play Billing (покупка coin), оформляя её как покупку сегментов, а не валюты.  
  - Логировать транзакции, синхронизировать их с сервером и `GameState`.

- **Онлайн‑фичи:**
  - Система “легендарных сегментов” и история владения с серверным хранением и публичным просмотром.  
  - События типа Early Wall Drop, ограниченные коллекции сегментов, глобальные акции.

---

---

## 📝 ПОСЛЕДНИЕ ОБНОВЛЕНИЯ (13 февраля 2026)

### Реализовано в последней сессии:

1. ✅ **Обваливающиеся платформы**
   - Тип платформ, которые обваливаются через 0.75 сек после приземления игрока
   - Генерация: 30% при старте, до 60% на высоте
   - Отключение коллизии сразу после первого касания
   - Визуальная индикация (красноватый оттенок)

2. ✅ **Оптимизация угла подъёма платформ**
   - Уменьшен `DY_STEP` с 64px до 10px (5% вместо 30%)
   - Улучшена видимость сегментов стены во время игры

3. ✅ **Формула роста цены по высоте**
   - Цена увеличивается на 10% каждые 1000 пикселей высоты
   - Максимальный множитель: 3x (на высоте 20000+ px)
   - Реализовано в `WallData.get_segment_price()`

4. ✅ **Визуальная подсветка сегментов**
   - Подсветка выбранного сегмента (белый оттенок)
   - Автоматическое снятие подсветки после покупки
   - Методы `set_highlighted_segment()` и `clear_highlight()` в `WallRenderer`

5. ✅ **Зум и перетаскивание камеры в CubeView**
   - Зум колесом мыши (от 0.1x до 5.0x)
   - Перетаскивание правой кнопкой мыши
   - Поддержка тач-жестов для мобильных
   - Учёт зума при подгрузке сегментов стены

6. ✅ **Мини-карта стены**
   - Визуализация стены через `MinimapDrawer.gd`
   - Отображение купленных сегментов (зелёные квадратики)
   - Индикатор позиции камеры (красный квадрат)
   - Статистика покупок в подписи

7. ✅ **Исправление критических ошибок**
   - Исправлен краш после исчезновения платформ (безопасное удаление из массива)
   - Исправлена ошибка парсера в `CubeView.gd` (проблема с типом переменной `side`)
   - Улучшена диагностика создания диалога покупки

8. ✅ **Статус в диалоге покупки**
   - Отображение владельца и даты покупки
   - Показ "Статус: Свободен" для непроданных сегментов

### Известные проблемы:

- ⚠️ Диалог покупки иногда не создаётся (`instantiate()` возвращает null)
  - Добавлена детальная диагностика и fallback на программное создание диалога
  - Требует дальнейшей отладки

- ⚠️ Мини-карта может не отображать визуализацию
  - Создан `MinimapDrawer.gd` для отрисовки через `_draw()`
  - Требует проверки работы в реальном времени

---

*Документ обновлён после реализации покупок, мини-карты, обваливающихся платформ, зума/навигации и исправления критических ошибок.*
